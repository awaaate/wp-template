query PostBySlug($slug: String!) {
    postBy(slug: $slug) {
        content
        excerpt
        title
        slug
        featuredImage {
            node {
                id
                sourceUrl
            }
        }
        seo {
            canonical
            metaDesc
            title
        }
    }
}
