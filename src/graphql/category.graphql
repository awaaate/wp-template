query CategoryById($id: ID!) {
    category(id: $id) {
        slug
        posts {
            edges {
                node {
                    id
                    slug
                    title
                    excerpt
                    featuredImage {
                        node {
                            sourceUrl
                        }
                    }
                    
                }
            }
        }
    }
}
